import styled from "styled-components";

export const ImageShadowContainer = styled.div`
    
		align-items: center;
}
`

export const ImageShadow = styled.img`
width: 300px;
-webkit-filter: brightness(0);
  filter: brightness(0);
 
`

